import { styled, Button } from "@mui/material";

export const StyledButton = styled(Button)({
  borderColor: "#C3D3E2",
  borderWidth: 2,
  borderRadius: 8,
  height: 40,
  textTransform: "none",
  fontWeight: "bold",
  fontSize: 13,
});
